# BUG 错题集（Mistake Book）

> 机制：每发现/修复一个真实 bug，**反思 → 归类 → 提炼同类排查点**，追加一条记录。
> 后续每一轮勘察时**优先通读错题集**，把其中的「同类排查点」当作主动排查清单，
> 去同类代码路径找类似 bug——靠历史教训，不靠运气。
> 与 `history`（根因去重，管"别重复报"）互补：错题集管**往哪挖**。

---

## 分类体系（12 大类）

| # | 类别 | 一句话识别特征 |
|---|------|----------------|
| 1 | **类型假设崩溃** | 代码假设字段是 str/int/None，实际是 list/dict/ISO字符串/枚举 |
| 2 | **取数方向/排序错** | `LIMIT+ASC` 取到最旧而非最新；顺序处理反了 |
| 3 | **脱敏/加解密失效** | 只脱敏顶层键；fail-open 把密文当明文/密钥泄漏到产物 |
| 4 | **状态一致性/竞态** | 平局自胜自败、运行中事件循环调用、并行消息互相覆盖 |
| 5 | **契约不匹配** | 生产者返回的键名/结构 与 消费者读取的 不一致 |
| 6 | **修复回归/漏同步** | 修复 A 打破 B；同根因只修了一处，另一处漏修 |
| 7 | **功能漂移** | 枚举/补全表没跟着新功能更新，CLI 支持但补全/表项缺失 |
| 8 | **验证盲区/静默放行** | 校验只看名字不看签名；单条坏数据拖垮整批；负缓存缺失 |
| 9 | **流/多事件丢结果** | 流式响应取第一个而非最后一个；progress 吞掉最终 result |
| 10 | **输入边界/正则** | 正则无边界吞掉邮箱/@提及；中句触发词剥离上下文 |
| 11 | **UI 视觉/交互违例** | 布局崩塌、文案截断、焦点陷阱、状态反馈缺失、对比度不足、响应式断裂 |
| 12 | **API 契约违例** | 状态码不符、响应结构错位、参数校验缺失、越权/未鉴权、注入成功、WS/GraphQL 协议异常 |

---

## 错题条目

<!-- 追加模板：复制下面到文件末尾填写 -->
<!--
### [分类序号] 一句话标题
- 现象：<一句话，bug 表现为>
- 根因：<为什么会产生这类错误>
- 反思：<这次哪里没想到；以后怎么避免>
- 同类排查点：<下次主动去哪些代码路径/输入域找同类 bug>
- 实例：<文件:行号 / findings_roundN.txt 引用>
-->

### [1] 字段类型假设太窄 → 解析崩溃
- 现象：对 `content`/`timestamp`/`task_type` 等字段直接 `float()`/`join()`/`.value`，遇到 list/ISO字符串/str 枚举即抛异常。
- 根因：解析层对"字段可能是多种形态"缺少防御；把"当前唯一调用方传入的类型"当成了"契约"。
- 反思：每处裸 `float()`/`"\n".join(x)`/`x.value`/`x["k"]` 都是潜在崩溃点；多模态/字符串枚举/脏数据是常态不是异常。
- 同类排查点：搜索代码里裸的类型转换与属性访问（`float(`、`int(`、`join(`、`.value`、`["timestamp"]`），重点看配置/消息/事件数据流入处。
- 实例：findings_round12（content 为 list）、round13（脏行 timestamp）、round5（ISO timestamp）、round7（字符串 task_type）。

### [2] 取最旧而非最新 → 恢复/续接丢最近消息
- 现象：会话/恢复路径用 `list_records(limit=N)`（timestamp ASC + LIMIT）取到"最旧 N 条"，续接的是一段对话开头而非结尾。
- 根因：分页默认升序的惯性；没意识到"LIMIT 取前 N"在 ASC 下是取最旧。
- 反思：凡"恢复/续接/回放最近状态"必查排序方向；limit 语义必须配 `ascending=False`。
- 同类排查点：搜索 `list_records`/`LIMIT`/`[:N]`/`.sort()`，凡恢复/续接/取最近场景核对是否取反。
- 实例：findings_round8（restore_tree）、round9（REPL 续接，同一根因第二处）。

### [3] 脱敏只做表层 / 加解密 fail-open → 密钥泄漏
- 现象：脱敏函数只检查顶层 key，嵌套 dict/list 里的 `api_key`/`Authorization` 原样保留进训练数据；解密函数在缺依赖时把密文当明文返回。
- 根因：脱敏/加密对"结构深度"和"依赖缺失"没做防御；默认 fail-open。
- 反思：凡是"导出/日志/序列化"路径的敏感字段处理，必须递归且 fail-closed（拿不准就当敏感）。
- 同类排查点：搜索 `_sanitize`/`_redact`/`decrypt`/`to_dict`/`export`/JSONL 序列化，检查递归深度与无依赖分支。
- 实例：findings_round16（嵌套密钥）、round20（无 fernet 返回密文）。

### [4] 竞态/并行导致状态自相矛盾
- 现象：同优先级并发动作 winner==loser（自胜自败）；运行中事件循环里调 `asyncio.run()`；并行 tool_call 消息互相覆盖只剩最后一个。
- 根因：对"同一把 key 的 max/min 返回首元素"、"已有事件循环"、"并行追加 vs 覆盖"缺少意识。
- 反思：凡是并行/并发/排序去极值，必须显式用元组次键破局并验证 winner≠loser；循环内跑同步驱动先查 running loop。
- 同类排查点：搜索 `max(`/`min(`/`sorted(`（并发仲裁）、`asyncio.run`（循环内）、`tool_calls`/`append` vs `=`（并行消息）。
- 实例：findings_round5（ConflictArbiter）、round15（drain_sync）、round8（tool_call 覆盖）。

### [5] 生产者/消费者契约键名不一致
- 现象：CLI 读 `total_tasks`，实际返回 `total_in_registry`；读 `result["result"]`，实际返回 `{stdout}`；消费者只在 should_proceed=False 时展示 caution。
- 根因：不同模块各写各的键名，没有对齐契约；警告通道的消费分支没覆盖全部返回值。
- 反思：跨模块读结构前先核对返回结构；"低置信度也要展示"这类分支最易漏。
- 同类排查点：搜索跨模块的 `["xxx"]`/`get("xxx")` 读取，对照生产者的 return dict；尤其 warning/caution/fallback 分支。
- 实例：findings_round7（task stats）、round7（cron stdout）、round18（caution 丢弃）。

### [6] 修复引入回归 / 同根因只修一处
- 现象：为排除邮箱给 `@` 引用加了路径形态过滤，反而拒绝 `@Makefile` 等无扩展名文件；同一取最近消息 bug 在 store 修了、REPL 续接漏修。
- 根因：修复时只考虑"要挡的坏输入"，没回归验证"合法输入仍可用"；同根因多处的修复没全量同步。
- 反思：任何修复都要问"会不会误伤合法输入"；找到根因后 grep 全仓找同类代码点一次修完。
- 同类排查点：修改正则/过滤逻辑后，跑一遍合法/边界双清单；修根因时 grep 关键词全仓同步。
- 实例：findings_round11（@Makefile 回归）、round9（REPL 漏修）、round5（experience_workshop 漏同步）。

### [7] 枚举/表项漂移，CLI 支持但补全/查询缺失
- 现象：`MEMORY_SUBCOMMANDS` 没含新增子命令，Tab 补全对 `pending/confirm/reject` 无候选。
- 根因：手维护的枚举/补全表与功能演进脱节。
- 反思：凡是"列出可用项"的静态表（补全、白名单、菜单），都要与真实注册表/路由对齐。
- 同类排查点：搜索硬编码的 `[...]` 子命令表、`completion_items`、白名单，与 CLI 路由逐一比对。
- 实例：findings_round6（memory 补全器）。

### [8] 校验看名字不看行为 → 每次必崩的"已验证"工具
- 现象：入口校验只看函数名与 AST 沙箱，不校验签名能收单位置 dict 参数；`def execute():`（无参）照样注册为可用工具。
- 根因：校验停留在"静态表面"（名字/语法），没做"最小行为验证"（真实调用一次）。
- 反思：凡"注册/晋升为可用"的校验，能真调用一次就真调用一次；静态通过 ≠ 可用。
- 同类排查点：搜索 `_validate_`/`validate_entrypoint`/probation/晋升路径，检查是否含真实调用。
- 实例：findings_round17（CodegenToolCreator）。

### [9] 流式/多事件只取第一个 → 丢最终结果
- 现象：MCP streamable-http 一次响应含 progress 通知 + 最终 result，解析到第一个 dict 就 return，把 progress 当结果。
- 根因：假设"一个响应一个 JSON"，没处理"多个事件需要选带 id 的最后一个"。
- 反思：流式解析必须区分"通知"与"应答"，取最后一个带 id/result 的，而非第一个。
- 同类排查点：搜索 SSE/流式解析 `json.loads` 循环、`return` 在循环内过早退出、`data:` 处理。
- 实例：findings_round14（_parse_sse_body）。

### [10] 正则/触发词无边界 → 吞掉合法内容或剥错上下文
- 现象：`@([^\s@]+)` 把邮箱 `me@example.com` 当文件引用；中句触发词把触发词前的整段真实指令剥掉，只剩「周五交」。
- 根因：正则/字符串查找没加边界断言，切割逻辑按"找到即截断"而非"只剥触发词本身"。
- 反思：任何 `find`+`strip` 组合都要区分"定位"与"切割范围"；正则先加 `(?<!\w)` 词边界。
- 同类排查点：搜索 `re.`（无 `\b`/`(?<!` 的）、`.find(`+`.strip(`/切片截断、触发词提取逻辑。
- 实例：findings_round9（邮箱吞并）、round4（中句触发词剥离）。

### [11] 单条脏数据拖垮整批处理
- 现象：持久化文件/事件库里一条坏行，让整个 `_load`/`replay`/`replay_topic` 批量处理崩溃，坏条目之后全丢。
- 根因：批量循环里没有逐条 try/except 的"部分失败恢复"，一条坏即全坏。
- 反思：凡批量读（加载/回放/迁移/同步），必须逐条容错 + 跳过坏行 + 告警，好行照常返回。
- 同类排查点：搜索批量 `_load`/`replay`/`for ... in rows`/`list_records` 的解析循环，确认有无逐条防护。
- 实例：findings_round13（事件回放）、round4（scheduler _load）。

### [12] UI 视觉/交互违例（浏览器渲染面）
- 现象：布局崩塌（遮挡/溢出/横向滚动条）、文案截断重叠、焦点陷阱/键盘不可达、loading/error/empty 状态视觉不可分、断点处布局断裂、对比度不足。
- 根因：视觉层没有"契约"意识——CSS/组件只过了"能显示"，没过"任何内容/任何尺寸/任何状态都正确显示"；无像素级回归护栏。
- 反思：UI 判定靠**截图 + DOM 几何断言**（`getBoundingClientRect` 数值），不靠"看着怪"；功能性视觉错误（溢出/焦点/状态）不依赖设计稿即可立案，审美差异才需要规范契约。
- 同类排查点：Playwright 起页 → 375/768/1024/1440 断点逐视口截图 → 塞长文本/CJK/emoji/空/超长数据 → 检查遮挡/溢出/焦点/状态色；对比度按 WCAG 4.5:1 计算。
- 实例：暂无（UI 面待首轮实战）。

### [13] API 契约违例（postmcp 接口面）
- 现象：状态码与业务语义不符（该 2xx 却 4xx、该 4xx 却 200）、响应结构错位（字段缺失/类型错/嵌套断裂）、必填缺失仍成功、无 token 可访问受保护资源（越权）、注入（SQLi/命令）在响应可观察、WebSocket 断连/消息乱序。
- 根因：后端对输入校验不严、鉴权/越权检查缺失、契约（OpenAPI/语义）与实际实现脱节；测试只覆盖 happy path，错误路径/边界无人验证。
- 反思：接口判定靠**断言驱动**——每个接口至少配状态码 + 关键字段断言，期望 vs 实际对照，不"看一眼说 OK"；发请求前必配 allowlist。
- 同类排查点：postmcp 导入 Swagger/Postman 拿全接口清单 → 参数边界（超长/空/负/NaN/类型错位）→ 越权/未鉴权 → 注入 → 状态码与结构断言 → WS 断连/超大消息；可与 Playwright 配合（同应用 API 层 + UI 层互证）。
- 实例：暂无（API 面待首轮实战）。

---

## 每轮使用规则（写进 agent 记忆）

1. **勘察前必读**：进入全量发现前，先通读本错题集，把「同类排查点」合并成本轮主动排查清单。
2. **发现后反思**：每确认一个真实 bug，按上面 12 类归到最贴近的一类，追加一条错题记录（现象/根因/反思/同类排查点/实例）。
3. **归类不重复**：同一根因不同文件算一条（合并）；新类别出现时在分类表新增。
4. **修复硬门槛**：`fixed` = TDD 红→绿 + **Live 真实环境复验通过**（原始复现命令重跑 / 同视口截图几何断言），缺 Live 证据只算单测绿，不算修好。
5. **工欲善其事·按测试类型配工具（本地优先 + 30 天有效期→搜索兜底）**：开工先排查工具清单——**先定测试类型**：黑盒（postmcp API + playwright UI + agent-tty/pexpect TUI + 数据库 MCP/CLI + fuzz/minimize + 系统 timeout/objdump/curl/tcpdump）、白盒（按项目语言：Java→mvn/JUnit/JaCoCo/SpotBugs/jstack，Python→pytest/coverage，Go→go test/cover，Rust→cargo/tarpaulin/cargo-fuzz/miri/geiger，**Rust 桌面 GUI→egui-driver（egui）/tauri-webdriver（Tauri macOS）/conduct（Tauri 跨平台）**）、自动化（项目测试命令 + 自研 fuzz/minimize/corpus 批量/最小化/差分）。**调研走「本地优先（≤30 天）→ 过期重搜 → 搜索兜底」**：先查 `.opencode/agent/tools-kb.md`——命中且验证日期 ≤30 天用本库（除非用户要求重搜），命中但 >30 天**必须重搜更新**（工具/生态会演进），未命中才真实搜索并**回写本库**（工具+来源+今天日期，知识持续积累）；禁止记忆库偷懒（跳过本库与搜索直接凭记忆）与过期知识复用（>30 天仍直接用）。有成熟开源工具就不自研轮子；**只装当前项目需要的工具**（不全部装），复杂项目多工具按模块分配协作；**换项目类型只换具体工具，测试类型框架不变**；场景→工具速查见 bug-hunter.md「工具清单与场景匹配」。
6. **兵马未动·知己知彼**：开工前先了解项目（是什么/什么功能/什么栈/入口/测试体系），按项目类型选工具，工具就绪才开工——三思而行、从不打无准备的仗；蒙眼开工/仓促上阵都是烧命。
7. **应并发，尽并发**：勘察/挖掘/验证/修复多方向并发推进，错题集的多个「同类排查点」同时打；除独占资源/顺序依赖外一律并行，别串行磨洋工。
8. **化整为零 + 先易后难 + 全覆盖宪法**：把审计范围拆成模块清单 `module-coverage.md`（每模块打难度/命中分），按难度升序先易后难调度、同难度内命中降序，卡住标「挂起」稍后补上；**全部模块必须 100% 覆盖**，死亡报告须 `已覆盖 Y/Y`，未覆盖/挂起须清零或有可豁免原因。
9. **测试分层 + 深度递进 + 覆盖引导 + 差分对照**：冒烟（秒级快筛）→ 聚焦（受影响模块）→ 全量（回归）；静态→动态→插桩→反汇编逐层加深；根因链完整才算挖透；异常样本用 `minimize_repro.py` 最小化；解析器类用 `fuzz_input.py` 变异矩阵并发轰（种子不足时 `corpus_fetch.py --lang <语言> --query "<项目类型>"` 并发搜**同类**开源项目扩充，或 `--repo <仓库>` 指定对照实现提取——种子贴合被测材质，命中率高于纯语言种子）；用 `coverage` 定位未覆盖路径定向挖；找等价实现做**差分对照**（单看目标看不错、对照显形的隐性 bug）；跨接口面**横向互证**（同根因多面异常合并成一条）。
10. **因地制宜 · 因材施教**：按目标形态选策略（白盒/黑盒/UI/PTY），按代码材质选手法（解析器→模糊、状态机→序列、并发→竞态、安全→越权、UI→截图断言），按环境/寿命调投入，轻方法挖不动立即升级；反模式：死板套流程。
11. **修复必须留痕**：每次修复写日志（修了什么/怎么修的/Live 复验）+ 每轮结束 git commit 提交（`fix(roundN): 摘要`）。
12. **模式分流**：全新启动询问用户是否自动修复——自动修复走 TDD+Live 全链路；只记录把 bug 写进 `bug-log.md`（不改码）。**恢复会话且未死亡（life>0）不询问**，沿用上次模式继续；bug-log 与 history 同为去重依据，清单内重复不计命。
13. **并行按量计命**：一轮并行发现多个非重复 bug 每项 +1（受 MAX_PER_ROUND 上限），两种模式都适用。
14. **死亡闭环**：寿命归 0 时先构建并保存 test-report.md（含下次重启建议）→ git 提交 → 输出死亡行 → 发起 `verify_life.py reset`（permission ask 弹窗授权，用户确认即放行）；agent 无权绕过 ask 自行重置。
15. **跨轮避旧**：错题集条目是"去哪挖"的线索，不是"去复现旧 bug"——新发现必须与 `history` 和 bug-log 根因去重后才计命。

## 2026-09-04 R1（boke 静态站）
### 1. 促销价填进"划线原价"字段
- 现象：月付列显示「现价 40 + 划线 7.9」，促销语义颠倒
- 根因：数据录入把首月促销价放 originalPrice（语义=划线原价），而 price 放常规月价；前端 priceCell 直接按"现价+划线原价"渲染
- 反思：跨字段语义约束（originalPrice≥price）应在生成端校验，前端也应防御性忽略倒挂数据
- 同类排查点：所有"price/originalPrice/discount"成对字段的数据集；折扣类字段方向性
- 实例：coding-tencent-lite 40/7.9 等 15 条
### 2. 响应式处理遗漏同族容器
- 现象：375px 整页横向溢出 67px
- 根因：.cat-tabs 有 overflow-x:auto 处理窄屏，但同族的 .billing-tabs（同为多 tab 行）漏了响应式处理
- 反思：修复一个组件的窄屏问题时要横向排查所有"同类结构"的容器
- 同类排查点：所有 width:fit-content 的横向 flex 容器在最小断点的表现
- 实例：.billing-tabs（4 枚计费 tab 固定不换行）
### 3. hash 路由只写不读后续变化
- 现象：浏览器后退后 URL=#token 但页面停在 video
- 根因：switchCategory 写 location.hash 制造了历史记录，却没有 hashchange 监听，前进/后退导航被忽略
- 反思：凡是往 location.hash 写值的代码，必须配对 hashchange 监听（写路由必监听路由）
- 同类排查点：所有 SPA 手写 hash 路由；pushState 无 popstate 监听
- 实例：switchCategory → location.hash = cat

## 2026-09-04 R2（boke 静态站）
### 4. hash fragment 当独立页面提交 sitemap
- 现象：sitemap.xml 6 条 #hash URL
- 根因：SPA hash 路由 ≠ 独立可索引页面，搜索引擎忽略 fragment
- 反思：sitemap 只收录「服务器返回不同内容的 URL」；hash 路由站点只有一条
- 同类排查点：所有 SPA 的 sitemap/robots/canonical 生成逻辑
- 实例：sitemap.xml #coding 等 6 条
### 5. 单位口径未标注（月均 vs 总价）
- 现象：列头「季付均价」显示月均值 $4.50，用户误读为季总价（差 3 倍）
- 根因：billingCycles.quarterly 存月均价，UI 列头没写口径；筛选器输入也同口径未标注
- 反思：任何「折算值」必须带上口径标签（月均/日均/折后），数字和单位不能分离
- 同类排查点：所有均价/折算列、筛选器与列的口径一致性
- 实例：xKiro quarterly=4.5/annual=4

## 2026-09-04 R3（boke 静态站）
### 6. 手工维护的摘要字段与明细数据脱节
- 现象：headline「8档/¥28 起」vs 实际 4 档 ¥39 起；kimi「¥39 起」实为已废弃首月价
- 根因：headline 手工写死，plans 增删改后不联动；促销价清理后残留
- 反思：凡是「摘要 vs 明细」成对的数据（headline/counts/起价），生成端必须有自动一致性校验（本例校验脚本 10 行即可全量兜底）
- 同类排查点：所有衍生统计字段（计数/最值/聚合）与源数据的一致性
- 实例：token-tencent、coding-kimi
### 7. 枚举扩展未同步消费端映射
- 现象：数据 badge 出现「售罄/团队」，前端映射缺失 fallback 灰色
- 根因：数据枚举与前端 BADGE_CLASS 双头维护，新增枚举无同步机制
- 反思：枚举值在数据端新增时，grep 消费端所有 switch/map；或消费端用显式默认+审计
- 同类排查点：BADGE_CLASS、quotaUnit unitMap、typeLabel 等所有枚举映射
- 实例：badge 售罄/团队

## 2026-09-04 R4（boke 静态站）
### 8. UI 重构后旧样式残留
- 现象：13 组 CSS 选择器（compare-bar/detail-card/plan-row/fb-chips 等 64 行）零引用
- 根因：前端从「多选对比条+单家详情卡」重构为「卡片+统一大表」，旧 CSS 留在样式表
- 反思：重构删组件时必须连带删样式（class 引用 grep 一遍）；死 CSS 增体积也易误导后续维护
- 同类排查点：所有重构过的前端项目——CSS 定义 vs 模板引用差集
- 实例：index.html 旧 UI 样式块

## 2026-09-04 R5（boke 静态站）
### 9. 双头映射表漏键（错题 7 变体）
- 现象：「已下架」分类统计显示「当前：undefined」
- 根因：CATEGORIES 加新分类时，catNames 独立维护的映射没同步；且含一个不可达的 relay 键掩盖了缺失
- 反思：同源映射只留一份（catNames 直接 = CAT_LABELS）；新增枚举时 grep 所有 "key 集合" 定义点
- 同类排查点：所有 X↔Y 映射对（CATEGORIES/CAT_LABELS/catNames/BADGE_CLASS/unitMap）
- 实例：renderHeroStats catNames
### 10. 周期字段丢失订阅语义
- 现象：周付订阅三处显示「一次性」买断价
- 根因：billingCycles.note 只按 periodDays==7 生成，不区分「每周续订」与「限购一次」
- 反思：时间段字段（7天/30天）生成的文案必须结合订阅性（billingLabel）判断；同一数值不同语义要在源头分流
- 同类排查点：所有 periodDays/cycle 相关展示文案；「一次性/永久/每月」类措辞与真实计费周期一致性
- 实例：coding-openstarry-plan2 星序版(周)

## 2026-09-04 R6（boke 静态站）
### 11. esc 纪律局部漏应用
- 现象：4 类数据插槽插入 innerHTML 未转义，注入载荷成为真实 DOM 元素
- 根因：契约注释写了「必须 esc」但散落在十余个渲染函数里，靠人肉记忆执行必有遗漏
- 反思：安全契约要么集中（统一渲染层），要么有工具兜底（CI grep 数据字段直接插值的模式）；本次用红测试注入法验证最可靠
- 同类排查点：所有 innerHTML 拼接点 ×JSON 字段矩阵；textContent 赋值安全
- 实例：modelCell concurrency/calls、compareHtml rating、priceCell inputPricePerMillion
### 12. 控件的禁用条件与其自身行为不一致
- 现象：重置按钮会清 billing 筛选，却在不含 billing 的 active 判定下被禁用
- 根因：active 判定与 reset 行为分别维护，行为扩展（加 billing tab）时判定没跟
- 反思：「disabled 条件」=「能被清除的状态集合非空」，两者必须同一来源或同处维护
- 同类排查点：所有 reset/clear 类按钮的禁用谓词与其清除范围的一致性
- 实例：fReset 与 filters.billing
