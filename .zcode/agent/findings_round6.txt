esc 纪律契约违规（潜在 XSS）：index.html:531 自声明「所有来自 JSON 数据、将插入 innerHTML 的字符串都必须经过 esc」，但 4 类数据插槽未转义——m.concurrency（字符串字段）、m.calls.toLocaleString()、prov.rating、plan.inputPricePerMillion（死字段）| 证据：红测试注入 concurrency='<img src=x onerror>' 后 DOM 出现真实 img 元素（index.html:866 修复前），calls='<b>injected</b>' 渲染为粗体元素 | 根因：esc 纪律在 modelCell/compareHtml 部分插槽漏应用；README 明确鼓励用户直接编辑 JSON，数据面非可信 | 修复：5 处补 esc/Number 约束，绿测试同载荷不再产生 DOM 元素、正常数字显示不变
fReset 与计费筛选状态矛盾：重置按钮 onclick 会清 filters.billing，但禁用判定 active 不含 billing——仅选 Credits 计费 tab 时重置按钮禁用，与其自身功能矛盾 | 证据：index.html:804 修复前 active 无 filters.billing；浏览器复现 billing-only 时 fReset.disabled=true | 修复：active 补 filters.billing，复验 billing-only 时可用且点击后 tab 复位
